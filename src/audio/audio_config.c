#include "audio_config.h"

const unsigned int num_audio_channels = NUM_AUDIO_CHANNELS;
const size_t num_audio_frames_per_buffer = NUM_AUDIO_FRAMES_PER_BUFFER;
const size_t audio_buffer_length = AUDIO_BUFFER_LENGTH;